package com.ecoassitant.back.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ReponsePossibleRepository extends JpaRepository<ReponsePossibleRepository, Long> {
}
